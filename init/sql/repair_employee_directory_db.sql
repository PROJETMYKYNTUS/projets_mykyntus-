-- Réparation idempotente si le volume Postgres a été créé avant employee_directory_db.
-- NE PAS exécuter tel quel — le mot de passe doit venir de l'environnement.
--
-- Exemple (PowerShell, depuis la racine du dépôt) :
--   $pwd = $env:KYNTUS_DB_PASSWORD_DIRECTORY
--   docker exec -i kyntus_db psql -U postgres -v ON_ERROR_STOP=1 -c "DO `$`$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'directory_user') THEN CREATE USER directory_user WITH PASSWORD '$pwd'; ELSE ALTER USER directory_user WITH PASSWORD '$pwd'; END IF; END `$`$;"
--
-- Puis appliquer les GRANT (voir init/docker-entrypoint-initdb.d/01-init-databases.sh).
